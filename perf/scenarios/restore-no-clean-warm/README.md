scenario that measures **user-visible warm rebuild time** after a cache snapshot restore. cold build populates the cache, soldr save/load round-trips it into a fresh location, then warm `cargo build` runs WITHOUT `cargo clean` — cargo's incremental fingerprint should accept the restored `target/` and skip rustc spawns. red here means snapshot fidelity is good but cargo's fingerprint check rejected the restored state, forcing re-invocations. compare sibling `cold-tar-untar-warm` which runs `cargo clean` to maximize cache-restore-fidelity measurement at the cost of conflating wrapper overhead with warm time. see zccache#348.
