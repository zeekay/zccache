# Depgraph Integration & Stress Tests

Integration and stress tests for the dependency graph crate.
Run with `uv run test --full` or `uv run cargo test -p zccache-depgraph --test stress_test -- --ignored`.
