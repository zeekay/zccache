// Empty lib target — allows other crates to declare zccache-cli as a
// dev-dependency so Cargo automatically builds the `zccache` binary and
// provides `CARGO_BIN_EXE_zccache` to their integration tests.
