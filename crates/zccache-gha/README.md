# zccache-gha

GitHub Actions Cache API client for zccache. Provides save/restore operations
using the native GHA artifact cache, eliminating the need for external
`actions/cache` steps in CI workflows.
